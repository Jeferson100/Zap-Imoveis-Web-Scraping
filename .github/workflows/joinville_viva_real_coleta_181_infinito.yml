name: Rodando Coleta Viva Real Joinville 181 Infinito
on: 

    workflow_run:
        workflows: ["Rodando Coleta Viva Real Joinville 91 180"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: joinville
      arquivo_rodando_imovel:  joinville_coleta_dados_viva_real_181_infinito.py