name: Rodando Coleta Zap Imoveis Balneario Picaras
on: 

    workflow_run:
        workflows: ["Rodando Coleta Viva Real Balneario Picaras"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: balneario_picaras
      arquivo_rodando_imovel:   balneario_picaras_coleta_dados_zap_imoveis.py