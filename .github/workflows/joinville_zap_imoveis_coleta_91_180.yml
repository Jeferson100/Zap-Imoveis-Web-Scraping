name: Rodando Coleta Zap Imoveis Joinville 91 180
on: 

    workflow_run:
        workflows: ["Rodando Coleta Zap Imoveis Joinville 0 90"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: joinville
      arquivo_rodando_imovel:  joinville_coleta_dados_zap_imoveis_91_180.py