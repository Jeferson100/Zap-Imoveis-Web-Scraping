name: Rodando Coleta Chave Mao Florianopolis
on: 

    workflow_run:
        workflows: ["Rodando Coleta Viva Real Florianopolis"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: florianopolis
      arquivo_rodando_imovel: florianopolis_coleta_dados_chave_mao.py