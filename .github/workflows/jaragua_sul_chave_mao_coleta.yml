name: Rodando Coleta Chave Mao Jaragua Sul
on: 

    workflow_run:
        workflows: ["Rodando Coleta Viva Real Jaragua do Sul"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: jaragua
      arquivo_rodando_imovel: jaragua_coleta_dados_chave_mao.py