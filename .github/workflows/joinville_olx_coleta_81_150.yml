name: Rodando Coleta Olx Joinville 81 150
on: 

    workflow_run:
        workflows: ["Rodando Coleta Olx Joinville 0 80"]
        types:
            - completed
    workflow_dispatch:    

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: joinville
      arquivo_rodando_imovel: joinville_coleta_dados_olx_81_150.py