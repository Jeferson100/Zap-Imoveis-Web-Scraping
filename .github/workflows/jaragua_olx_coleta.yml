name: Rodando Coleta OLX Jaragua
on: 

    workflow_run:
        workflows: ["Rodando Coleta Chave Mao Jaragua"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: jaragua
      arquivo_rodando_imovel: jaragua_coleta_dados_olx.py