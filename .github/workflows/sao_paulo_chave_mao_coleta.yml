name: Rodando Coleta Chave Mao Sao Paulo
on: 

    workflow_run:
        workflows: ["Rodando Coleta Viva Real Sao Paulo"]
        types:
            - completed
    workflow_dispatch:  

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: sao_paulo
      arquivo_rodando_imovel:  sao_paulo_coleta_dados_chave_mao.py