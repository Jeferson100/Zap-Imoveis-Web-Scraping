name: Rodando Coleta OLX Curitiba
on: 

    workflow_run:
        workflows: ["Rodando Coleta Chave Mao Curitiba"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: curitiba
      arquivo_rodando_imovel: curitiba_coleta_dados_olx.py