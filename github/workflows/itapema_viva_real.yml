name: Rodando Coleta Viva Real Itapema
on: 

    workflow_run:
        workflows: ["Rodando Coleta Zap Imoveis Itapema"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: itapema
      arquivo_rodando_imovel:  itapema_coleta_dados_viva_real.py