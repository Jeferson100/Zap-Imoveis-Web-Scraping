name: Rodando Coleta OLX Itajai
on: 

    workflow_run:
        workflows: ["Rodando Coleta Chave Mao Itajai"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: itajai
      arquivo_rodando_imovel: itajai_coleta_dados_olx.py