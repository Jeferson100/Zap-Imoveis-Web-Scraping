name: Rodando Coleta Chave Mao Blumenau
on: 

    workflow_run:
        workflows: ["Rodando Coleta Viva Real Blumenau"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: blumenau
      arquivo_rodando_imovel: blumenau_coleta_dados_chave_mao.py