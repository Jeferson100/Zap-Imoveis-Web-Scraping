name: Rodando Coleta Olx Joinville
on: 

    workflow_run:
        workflows: ["Rodando Coleta Chave Mao Joinville"]
        types:
            - completed
    workflow_dispatch:    

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: joinville
      arquivo_rodando_imovel: joinville_coleta_dados_olx.py