name: Rodando Coleta Chave Mao Curitiba
on: 

    workflow_run:
        workflows: ["Rodando Coleta Viva Real Curitiba"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: curitiba
      arquivo_rodando_imovel: curitiba_coleta_dados_chave_mao.py