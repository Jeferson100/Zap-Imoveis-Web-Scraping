name: Rodando Coleta Chave Mao Joinville 
on: 

    workflow_run:
        workflows: ["Rodando Coleta Viva Real Joinville"]
        types:
            - completed
    workflow_dispatch:  

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: joinville
      arquivo_rodando_imovel:  joinville_coleta_dados_chave_mao.py