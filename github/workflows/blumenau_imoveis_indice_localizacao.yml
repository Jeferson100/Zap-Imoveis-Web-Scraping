name: Rodando Criando Indice Localizacao Imoveis Blumenau
on: 

    workflow_run:
        workflows: ["Rodando Limpeza Imoveis Blumenau"]
        types:
            - completed
    workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/pipeline_cidade_rodando.yml
    with:
      cidade_pasta: blumenau
      arquivo_rodando_imovel: blumenau_indice_localizacao.py